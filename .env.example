# Client API target
NEXT_PUBLIC_API_URL=http://localhost:5000

# Keep JWT, MongoDB, Resend, and Cloudinary credentials in the separate server
# repository. Never place private values in NEXT_PUBLIC_* variables.
